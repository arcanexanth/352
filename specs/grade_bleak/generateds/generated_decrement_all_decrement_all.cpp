TEST(BleakVirtualMachine, decrement_all_decrement_all) {
  BleakVirtualMachine vm("cases/decrement_all.bleak", "cases/decrement_all.in");
  for (int reset = 0; reset < 5; ++reset) {
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(0, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 0 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 1 step, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 2 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 3 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 4 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 5 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 6 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 7 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 8 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 9 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 10 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 11 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 12 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 13 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 14 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 15 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 16 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 17 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 18 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 19 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 20 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 21 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 22 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 23 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 24 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 25 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 26 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 27 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 28 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 29 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 30 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 31 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(4, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 32 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(1, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 33 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(2, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 34 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(3, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(-1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 35 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(5, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(-1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 36 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(1000, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 37 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 38 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(8, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 39 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 40 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(900, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 41 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 42 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(7, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 43 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 44 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(800, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 45 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 46 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(6, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 47 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 48 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(700, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 49 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 50 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(5, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 51 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 52 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(600, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 53 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 54 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(4, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 55 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 56 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(500, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 57 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 58 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(3, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 59 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 60 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(400, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 61 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 62 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(2, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 63 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 64 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(300, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 65 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 66 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(8, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(1, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 67 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(9, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 68 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(6, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(200, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 69 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(7, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(199, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 70 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({}), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(10, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(199, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 71 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    vm.Step();
    ASSERT_EQ(vector<string>({"store r9 8", "input [r9] <- read", "dec r9", "jneg r9 next", "jmp read", "store r9 8 <- next", "dec [r9] <- decrement", "jzilch r9 done", "dec r9", "jmp decrement", "output 0 <- done", }), vm.GetInstructions()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInstructions didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({1000, 900, 800, 700, 600, 500, 400, 300, 200, }), vm.GetInput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetInput didn't give back the list I expected.";
    ASSERT_EQ(vector<int>({0, }), vm.GetOutput()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetOutput didn't give back the list I expected.";
    {
      map<string, int> expected_labels = {{"decrement", 6}, {"done", 10}, {"next", 5}, {"read", 1}, };
      ASSERT_EQ(expected_labels, vm.GetLabels()) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but GetLabels didn't give back the map I expected.";
    }
    ASSERT_EQ(11, vm.GetRegisters().find("pc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register pc wasn't what I expected it to be.";
    ASSERT_EQ(9, vm.GetRegisters().find("nc")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register nc wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("ra")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register ra wasn't what I expected it to be.";
    ASSERT_EQ(199, vm.GetRegisters().find("r0")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r0 wasn't what I expected it to be.";
    ASSERT_EQ(299, vm.GetRegisters().find("r1")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r1 wasn't what I expected it to be.";
    ASSERT_EQ(399, vm.GetRegisters().find("r2")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r2 wasn't what I expected it to be.";
    ASSERT_EQ(499, vm.GetRegisters().find("r3")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r3 wasn't what I expected it to be.";
    ASSERT_EQ(599, vm.GetRegisters().find("r4")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r4 wasn't what I expected it to be.";
    ASSERT_EQ(699, vm.GetRegisters().find("r5")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r5 wasn't what I expected it to be.";
    ASSERT_EQ(799, vm.GetRegisters().find("r6")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r6 wasn't what I expected it to be.";
    ASSERT_EQ(899, vm.GetRegisters().find("r7")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r7 wasn't what I expected it to be.";
    ASSERT_EQ(999, vm.GetRegisters().find("r8")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r8 wasn't what I expected it to be.";
    ASSERT_EQ(0, vm.GetRegisters().find("r9")->second) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 72 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but register r9 wasn't what I expected it to be.";
    bool saw_exception = false;
    try {
      vm.Step();
    } catch (int actual_exception) {
      saw_exception = true;
      ASSERT_EQ(BleakVirtualMachine::INSTRUCTION_OUT_OF_BOUNDS, actual_exception) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 73 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but the exception raised on the last call to Step wasn't what I expected.";
    }
    ASSERT_TRUE(saw_exception) << "I tried stepping a virtual machine on source cases/decrement_all.bleak and input cases/decrement_all.in 73 steps, having reset " << reset << " time" << (reset == 1 ? "" : "s") << ", but the last call to Step didn't raise exception INSTRUCTION_OUT_OF_BOUNDS as I expected.";
    vm.Reset();
  }
}

